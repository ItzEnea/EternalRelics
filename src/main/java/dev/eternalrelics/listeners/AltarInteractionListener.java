package dev.eternalrelics.listeners;

import dev.eternalrelics.EternalRelicsPlugin;
import org.bukkit.event.Listener;

public class AltarInteractionListener implements Listener {
    private final EternalRelicsPlugin plugin;
    public AltarInteractionListener(EternalRelicsPlugin plugin) { this.plugin = plugin; }
}
